name: PR Labeler

on:
  - pull_request_target

jobs:
  labeler:
    name: Label
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest

    steps:
      - uses: actions/labeler@v6.1.0
        with:
          sync-labels: true
