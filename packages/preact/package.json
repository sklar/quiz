{
	"name": "@quiz/preact",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"clean": "rm -rf dist && rm -rf node_modules && rm *.tsbuildinfo",
		"preview": "vite preview",
		"lint.check": "pnpm biome check .",
		"lint.write": "pnpm biome check --write .",
		"type.check": "tsc --noEmit"
	},
	"dependencies": {
		"open-props": "^1.7.9",
		"party-js": "^2.2.0",
		"preact": "^10.25.3"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.9.3",
		"vite": "^5.4.11"
	},
	"volta": {
		"extends": "../../package.json"
	}
}
