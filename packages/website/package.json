{
	"name": "@quiz/website",
	"type": "module",
	"version": "1.0.2",
	"scripts": {
		"astro": "astro",
		"dev": "astro dev",
		"build": "astro check && astro build",
		"clean": "rm -rf dist && rm -rf node_modules",
		"preview": "astro preview",
		"lint.check": "pnpm biome check .",
		"lint.write": "pnpm biome check --write .",
		"type.check": "astro check sync && tsc --noEmit"
	},
	"dependencies": {
		"astro": "^5.18.0",
		"astro-icon": "^1.1.5",
		"open-props": "^1.7.23"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.6"
	},
	"volta": {
		"extends": "../../package.json"
	}
}
